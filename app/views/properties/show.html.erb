    <div class="container">
      <div class="row">
        <div class="col-xs-12">
            <div class="col-lg-8 col-md-8 col-sm-12">
              <!-- 2. Add images to <div class="fotorama"></div>. -->
              <div class="fotorama"
                   data-nav="thumbs"
                   data-width="100%"
                   data-ratio="800/600"
                   data-thumb-height"46">

                   <% if @pictures.count > 0 %>
                      <% @pictures.each do |picture|%>
                        <a href="<%= picture.file.thumb('730x548#').url %>">
                          <img src="<%= picture.file.thumb('98x64#').url %>">
                        </a>
                      <% end %>
                    <% end %>
              </div>
              <!-- // GALLERY START // -->
              <!-- 
                IMPORTANT NOTE: Each property gallery must have an unique ID for the gallery 
                        function to work properly. This unique ID must reflect the folder 
                        name which contains all the corresponding property images.
                        
                        **Please refer to the documentation for further usage:
                        * IMPORTANT:
                        * id   = [PLACE IMAGE FOLDER NAME] 
                        * data = [PLACE TEXT TO BE DISPLAYED FOR ATTENTION - LEAVE "" FOR BLANK]
                        * num  = [NUMBER OF IMAGES AND CORRESPONDING THUMBNAILS WHICH WILL BE DISPLAYED]
              -->
              
              <!-- <div class="gallery"  id="property_name" data-sticker="HOT" data-num="20">
                <p>Please enable your javascript to view this gallery</p>
              </div>
              <! gallery end -->
              
              <!-- about property --> 
              <div class="row-fluid property-info">
                  <% unless @property.description.nil? || @property.description.length < 1  %>
                    <h4>Sobre:</h4>
                  
                    <p class="description">
                      <%= @property.description %>
                    </p>
                  <% end %>
                  
              </div>
              <!-- end about property --> 
            </div>
            <!-- end span8 -->  
            
            <div class="col-lg-4 col-md-4 col-sm-8">
              <!--key features-->
              <h2>
                <span class="label label-info">
                  Código de referência: <%= @property.id %>
                </span>
              </h2>
              <h4>Características do imóvel:</h4>
              <div class="property-info">
              <ul class="property-info">
                <% if @property.suits > 0 %>
                  <li><strong>Suites:</strong> <%= @property.suits %> </li>
                <% end %>
                <% if @property.bedrooms > 0 %>
                  <li><strong>Quartos:</strong> <%= @property.bathrooms %> </li>
                <% end %>
                <% if @property.bathrooms > 0 %>
                  <li><strong>Banheiros:</strong> <%= @property.bathrooms %> </li>
                <% end %>
                <% if @property.garages > 0 %>
                  <li><strong>Vagas de garagem:</strong> <%= @property.bathrooms %> </li>
                <% end %>
                <li><strong>Categoria :</strong> <%= @property.property_type.name %></li>
                <li><strong>Location :</strong> <%= @property.district.name %>, <%= @property.district.city.name %> - <%= @property.district.city.state.uf %></li>
                <li><strong>Floor Size :</strong> 2269 sq.m.</li>
              </ul>
              <p class="asking-price">
                  <h2>
                    <span class="label label-default">
                      <% if @property.sell_price %>
                        Valor: <%= number_to_currency(@property.sell_price) %>
                      <% else %>
                        Consulte o valor
                      <% end %>
                    </span>
                  </h2>
                  </p>
              </div>
              
              <!--divider-->
              <!-- <div class="divider"></div> -->
              
              <!--single map-->
              <!-- <h4>Localização:</h4>
              <div id="map_canvas"></div> -->
              <br>
              <!--divider-->
              <div class="divider"></div>
              <!-- contact agent form -->
              <div class="col-xs-12 col-sm-12 col-md-12">
                <%= render "shared/contact_property"%>
              </div>
            </div>
            </div>
          </div><!-- end row-fluid -->

          <br>

    </div> <!-- /container -->